version: 2
updates:
    - package-ecosystem: 'npm'
      directory: '/packages/django_hstore_widget'
      schedule:
          interval: 'daily'

    - package-ecosystem: 'uv'
      directory: '/'
      schedule:
          interval: 'daily'

    - package-ecosystem: 'github-actions'
      directory: '/'
      schedule:
          interval: 'daily'
